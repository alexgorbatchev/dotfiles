---
mode: agent
---

Use bulk read to consume files in `packages` and `docs/tool-configuration`, skip `.test.ts` files. Read carefuly through all of the code and the docs, identify where docs need to be corrected and updated,
then update them. pipe the bulk read output into a single file, then read it using built in tools.

DO NOT LET the code to be printed to stdout.
