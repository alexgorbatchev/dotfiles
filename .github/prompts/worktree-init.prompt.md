---
agent: agent
---

To setup a new worktree do the following:
- new worktrees should be placed in the `.tmp/worktrees` directory
- copy .npmrc from the original worktree to the new worktree
- run `bun install`
