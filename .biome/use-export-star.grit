engine biome(1.0)
language js(typescript,jsx)

JsExport() as $export_stmt where {
    $export_stmt <: contains JsExportNamedFromClause(),
    register_diagnostic(
        span = $export_stmt,
        message = "Use `export * from` instead of `export { ... } from` for better maintainability."
    )
}
