# @dotfiles/installer-dmg

DMG installer plugin for the dotfiles tool installer system. Downloads macOS .dmg disk images, mounts them, copies .app bundles, and symlinks binaries. Silently skips on non-macOS platforms.
