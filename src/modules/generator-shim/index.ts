/**
 * @file Barrel file for the Shim Generator module.
 *
 * ## Development Plan
 *
 * - [x] Export `IShimGenerator` from `./IShimGenerator`.
 * - [x] Export `ShimGenerator` from `./ShimGenerator`.
 * - [ ] Write tests for the module (covered by ShimGenerator.test.ts).
 * - [ ] Cleanup all linting errors and warnings.
 * - [ ] Cleanup all comments that are no longer relevant (leaving development plan).
 * - [ ] Ensure 100% test coverage for executable code.
 * - [ ] Update the memory bank with the new information when all tasks are complete.
 */

export * from './IShimGenerator';
export * from './ShimGenerator';
